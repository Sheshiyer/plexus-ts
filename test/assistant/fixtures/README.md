# Assistant Fixtures

Fixtures in this directory are deterministic and safe for local tests. They use
mock database rows, mock model providers, and sample app state only; they never
call live AI APIs or read a real Plexus database.
